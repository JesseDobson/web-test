1. under org.naic.mfl.se.challenge right click webtest.java run as testng test (there is some authentication issue caused by problem with signup workflow, ideally I would debug it)

2. Additional exe is chrome driver in path specified in instructions

3. TestNG is added as maven dependency, javadoc is from attaching testng-sources-6.11 jar as java doc. These are only added libraries